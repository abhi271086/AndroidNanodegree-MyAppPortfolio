<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scrollbars="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.anahar.myportfolioproject.MainActivity"
    tools:showIn="@layout/activity_main"
    android:background="#aee3eb">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/app_name"
        android:id="@+id/app_label"
        android:autoText="false"
        android:editable="false"
        android:inputType="text"
        android:textColor="#414094"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/abc_text_size_display_1_material"
        android:cursorVisible="false" />

    <Button
        style="@style/MyAppButtons"
        android:text="@string/spotify_label"
        android:id="@+id/spotify_button"
        android:layout_below="@+id/app_label"
  />
    <!-- android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#f57c00"
    android:textSize="@dimen/abc_action_bar_content_inset_material"
    android:layout_centerHorizontal="true"
    android:textAlignment="center"
    android:onClick="sendMessage"-->

<Button
    style="@style/MyAppButtons"
    android:text="@string/super_duo_label"
    android:id="@+id/super_duo_button"
    android:layout_below="@+id/spotify_button"
    />
    <!--android:layout_width="wrap_content"
    android:layout_alignLeft="@+id/spotify_button"
    android:layout_alignStart="@+id/spotify_button"
    android:layout_alignRight="@+id/spotify_button"
    android:layout_alignEnd="@+id/spotify_button"

    android:layout_height="wrap_content"
    android:background="#f57c00"
    android:textSize="@dimen/abc_action_bar_content_inset_material"
    android:layout_marginTop="36dp"
    android:textAlignment="center"
    -->

<Button
    style="@style/MyAppButtons"
    android:text="@string/build_bigger_label"
    android:id="@+id/build_bigger_button"
    android:layout_below="@+id/super_duo_button"
    />
    <!--android:background="#f57c00"
    android:textSize="@dimen/abc_action_bar_content_inset_material"
    android:textAlignment="center"

    android:layout_alignLeft="@+id/super_duo_button"
    android:layout_alignStart="@+id/super_duo_button"
    android:layout_marginTop="45dp"
    android:layout_alignRight="@+id/super_duo_button"
    android:layout_alignEnd="@+id/super_duo_button" -->

    <Button
        style="@style/MyAppButtons"
        android:text="@string/xyz_reader_label"
        android:id="@+id/xyz_reader_button"
        android:layout_below="@+id/build_bigger_button"
        />
    <!--
        android:layout_alignLeft="@+id/build_bigger_button"
        android:layout_alignStart="@+id/build_bigger_button"
        android:layout_alignRight="@+id/build_bigger_button"
        android:layout_alignEnd="@+id/build_bigger_button" -->

<Button
    style="@style/MyAppButtons"
    android:text="@string/capstone_label"
    android:id="@+id/capstone_button"
    android:background="#f11313"
    android:textColor="#ffffff"
    android:onClick="showToastMessage"
    android:layout_below="@+id/xyz_reader_button"
    />
    <!--
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="46dp"

    android:textSize="20dp"

    android:layout_centerHorizontal="true"
    -->

</RelativeLayout>
